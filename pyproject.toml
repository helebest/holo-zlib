[project]
name = "holo-zlib"
version = "0.1.0"
description = "Z-Library search & download skill for Claude Code"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
